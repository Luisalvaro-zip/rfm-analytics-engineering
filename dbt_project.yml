name: 'retail_ecommerce_dbt'
version: '1.0.0'
config-version: 2

profile: 'retail_ecommerce_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  retail_ecommerce_dbt: # <-- ESTE NOMBRE DEBE SER IDÉNTICO AL 'name' DE ARRIBA
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: ephemeral
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts